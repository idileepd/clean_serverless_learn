service: my-gql

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  environment:
    MONGODB_URI: mongodb+srv://dileep:qwvW9YKn9DxzIKkr@lme-serverless-db.jyxp4ak.mongodb.net/db
    JWT_SECRET: my-ultra-secure-and-ultra-long-secret
    JWT_EXPIRES_IN: 90d
    USE_COGNITO: false

plugins:
  - serverless-webpack

custom:
  webpack:
    webpackConfig: "./webpack.config.js"
    includeModules: true

functions:
  auth-gql:
    handler: src/auth-gql-sls/graphql.handler
    events:
      - http:
          path: auth-gql
          method: post
          cors: true
